import {SymbolEnum} from '../../lib';

type RandomizeModuleTypes = 'RandomizeModule';

export const RANDOMIZE_MODULES: SymbolEnum<RandomizeModuleTypes> = {
   RandomizeModule: Symbol.for('RandomizeModule')
};