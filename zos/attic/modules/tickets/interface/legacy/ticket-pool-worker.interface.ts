export interface ITicketPoolWorker
{
   updateMissingContent(): void;

   screenContent(): void;
}

   // submitRender(buffer: Buffer, index: number): void;
   //
   // approveRender(buffer: Buffer, index: number): void;
   //
   // rejectRender(buffer: Buffer, index: number): void;
// }